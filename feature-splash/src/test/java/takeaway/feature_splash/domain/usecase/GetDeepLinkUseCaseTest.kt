package takeaway.feature_splash.domain.usecase

import org.junit.runner.RunWith
import org.mockito.junit.MockitoJUnitRunner

@RunWith(MockitoJUnitRunner::class)
class GetDeepLinkUseCaseTest {

    private val useCase = GetDeepLinkUseCase()

    //TODO(Написать нормально)
//    @Test
//    fun `invoke EXPECT deep link`() {
//        val deepLinkStr = "promo"
//        val result = DeepLink("ds", emptyMap())
//
//        assertEquals(useCase(deepLinkStr), result)
//    }
}