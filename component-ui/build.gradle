apply plugin: 'kotlin-kapt'

dependencies {
    //Support
    implementation(supportLibraries.appCompat)
    implementation(supportLibraries.ktx)

    //glide
    implementation(libraries.glide)
    kapt(libraries.glideCompiller)

    //UI
    implementation(libraries.material)
}