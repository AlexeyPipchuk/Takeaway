package takeaway.shared_phone_prefix.data.datasource

import org.junit.Ignore
import org.junit.Test
import org.junit.runner.RunWith
import org.mockito.junit.MockitoJUnitRunner

@RunWith(MockitoJUnitRunner::class)
class PhoneCountryDataSourceTest {

    //private val context: Context = mock()
    //private val dataSource = PhoneCountryDataSource(context)

    @Test
    @Ignore("impossible with library")
    fun `get phone country prefix EXPECT phone country prefix`() {}
}