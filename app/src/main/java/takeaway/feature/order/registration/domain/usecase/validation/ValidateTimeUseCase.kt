package takeaway.feature.order.registration.domain.usecase.validation

import javax.inject.Inject

class ValidateTimeUseCase @Inject constructor() {

    operator fun invoke(date: String): String? = null
}